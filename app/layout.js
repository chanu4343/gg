export const metadata = { title: "음식키우기 — 시험용" };

export default function RootLayout({ children }) {
  return (
    <html lang="ko">
      <body className="bg-slate-50 min-h-screen">{children}</body>
    </html>
  );
}
